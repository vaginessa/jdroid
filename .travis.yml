language: android

sudo: false

jdk:
  - oraclejdk7

android:
  components:

    # The BuildTools version used by your project
    - build-tools-23.0.2

    # The SDK version used to compile your project
    - android-23

before_script:
    - android list sdk --no-ui --all --extended

script: sh ./etc/scripts/test.sh true false true false false
